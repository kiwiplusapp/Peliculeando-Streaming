import { OlimpoContent } from '@/components/media/OlimpoContent';

export const metadata = { title: 'El Olimpo — Peliculeando' };

export default function ComunidadPage() {
  return <OlimpoContent />;
}
